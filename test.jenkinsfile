#!groovy

def hostsIPsMap = [
  'QA1': '10.10.10.10',
  'QA2': '20.20.20.20'



]


def SERVER_DEPLOY=null 
SERVER_DEPLOY = hostsIPsMap[]

import parameters.choiceParam 

pipeline { 
    agent any 
    

    stages {
        stage('Build') { 
            steps { 
                echo 'holahola' 
            }
        }
        stage('Deploy') {
            steps {

              script{
              print "SERVER_DEPLOY : " + SERVER_DEPLOY[choiceParam]
               if ("${SERVER_DEPLOY}" == '10.10.10.10') {
                 sh '''
                 ls
                 '''
               } else {
                 sh '''
                 ls -l
                 '''
                  }
               }
            }
        }
    }
}
