#!groovy


def hostsIPsMap = [
  'QA1': '10.10.10.10',
  'QA2': '20.20.20.20'



]


def SERVER_DEPLOY=null 
SERVER_DEPLOY = [env.hostsIPsMap]




pipeline { 
    agent any 
    

    stages {
        stage('Build') { 
            steps { 
                echo 'holahola' 
            }
        }
        stage('Deploy') {
            steps {

              script{
              print "SERVER_DEPLOY : " + env.hostsIPsMap
               if ("${SERVER_DEPLOY}" == '10.10.10.10') {
                 sh '''
                 ls
                 '''
               } else {
                 sh '''
                 ls -l
                 '''
                  }
               }
            }
        }
    }
}
