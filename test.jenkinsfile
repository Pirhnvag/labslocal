#!groovy
pipeline { 
    agent any 
    stages {
        stage('Build') { 
            steps { 
                echo 'holahola' 
            }
        }
        stage('Deploy') {
            steps {
              script{
              print "SERVER_DEPLOY : " + SERVER_DEPLOY
               if ("${SERVER_DEPLOY}" == '10.10.10.10') {
                 sh '''
                 ls
                 '''
               } else {
                 sh '''
                 ls -l
                 '''
                  }
               }
            }
        }
    }
}
