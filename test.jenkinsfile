#!groovy


def hostsIPsMap = [
  'QA1': '10.10.10.10',
  'QA2': '20.20.20.20',
  'QA3': '30.30.30.30',
  'QA4': '40.40.40.40',
  'QA5': '50.50.50.50',
  'QA6': '60.60.60.60',
  'QA7': '70.70.70.70',
  'QA8': '80.80.80.80',
  'QA9': '90.90.90.90'
]
def SERVER_DEPLOY=null 
SERVER_DEPLOY = env.seleccioneAmbiente

pipeline { 
    agent any
    //parameters {
    //gitParameter branchFilter: 'origin/(.*)', defaultValue: 'master', name: 'BRANCH', type: 'PT_BRANCH'
  //}  
    stages {
      stage('Checkout') {
      steps {
        git branch: env.defaultValue, url: 'https://github.com/Pirhnvag/labslocal.git'
      }
    }
      stage('Deploy') {
        steps {
          script{
            print "SERVER_DEPLOY: " + env.seleccioneAmbiente
            if ("${SERVER_DEPLOY}" == 'QA1') {
            sh '''
            echo 'Realizando despliegue en ambiente QA1'
                 '''
               }
            else if ("${SERVER_DEPLOY}" == 'QA2')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA2'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA3')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA3'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA4')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA4'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA5')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA5'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA6')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA6'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA7')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA7'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA8')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA8'
                 '''
                  }
            else if ("${SERVER_DEPLOY}" == 'QA9')  {
            sh '''
            echo 'Realizando despliegue en ambiente QA9'
                 '''
                  }
               }
            }
        }
    }
}
