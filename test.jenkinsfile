#!groovy
def hostsIPsMap = [
  'QA1': '10.10.10.10',
  'Q2': '20.20.20.20'
]
def SERVER_DEPLOY=null
SERVER_DEPLOY = hostsIPsMap[DESTINO]

pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }

    stages {
        stage('Build') { 
            steps { 
                sh 'ls' 
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls -ltr'
            }
        }
    }
}
